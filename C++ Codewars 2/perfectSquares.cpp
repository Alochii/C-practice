#include <iostream>
#include <cmath>
#include <array>
#include <vector>
/*using namespace std;
std::vector<std::pair<unsigned int, unsigned int>> nClosest(const unsigned int n, const unsigned int k) {
    std::vector<std::pair<unsigned int,unsigned int>> vector[][2] = {{1,2}};
    unsigned int i = 1;
    return vector;
}
int squares(int n){
    int track = 0;
    for (double i = n-1; i>1; i--){
        for (double j = i-1; j>0; j--){
            if (sqrt(i+j) == static_cast<int>(sqrt(i+j)) && sqrt(i-j) == static_cast<int>(sqrt(i-j))){
                cout << "this one is the answer"<<endl<<i<< " "<<j<<endl;
                track +=1;
                cout <<"track : "<< track<<endl;
                if (track >=5){
                return 0;
                }
            }
        }
    }
    return 0;
}
int main(){
    cout << squares(1000);
    return 0;
}
*/