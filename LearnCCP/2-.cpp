#include <iostream>

using namespace std;

int main(){
    int first;
    int second;
    cin >> first;
    cin >> second;
    int summation = first + second;
    cout << "the sum of " <<first <<" and "<<second<<" is : " << summation;
    return 0;
}